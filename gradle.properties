org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=false
org.gradle.parallel=true
org.gradle.caching=true

# Minecraft / NeoForge
minecraft_version=1.21.1
minecraft_version_range=[1.21.1,1.21.2)
neo_version=21.1.247
neo_version_range=[21.1.0,)
loader_version_range=[4,)

# Parchment mappings (optional, but nice readable parameter names)
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Curios API (optional soft-dependency - compileOnly, never bundled)
curios_version=9.5.1+1.21.1

# Mixin annotation processor (for the optional Elytra Slot compatibility mixin)
mixin_version=0.8.5

# Mod metadata
mod_id=elytratoggle
mod_name=Elytra Toggle
mod_license=MIT
mod_version=1.5.0
mod_group_id=com.example.elytratoggle
mod_authors=You
mod_description=Bind a key to toggle elytra flight on and off.
